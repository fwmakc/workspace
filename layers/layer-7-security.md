# Layer 7 — Security | Безопасность

Единый документ по безопасности Workspace. Собран из всех слоёв проектирования: от пользовательского опыта до технической реализации.

---

## Принцип

Workspace проектируется с подходом **security by default**:

- **Без паролей** — аутентификация на ключевых парах (Ed25519). Пользователь никогда не вводит пароль
- **Данные у пользователя** — на его дисках, в его квартире. Не на серверах корпораций
- **Бэк — источник истины** — Фронт не контролирует доступ, только запрашивает
- **Изоляция по умолчанию** — приложения в V8 Isolates, Space изолированы друг от друга
- **Минимальная поверхность атаки** — один открытый порт (WireGuard), минимум системных вызовов

---

## 1. Аутентификация

### 1.1 Устройства — без паролей

Всё на ключевых парах (Ed25519):

1. Устройство генерирует ключевую пару (Ed25519)
2. Публичный ключ отправляется на Бэк при приглашении
3. Приватный ключ хранится на устройстве (TPM/Secure Enclave если есть)
4. Подключение = подпись challenge приватным ключом
5. Пароли не используются

**Master key pair:**
- Генерируется при первом входе
- Master key зашифрован биометрией/PIN устройства
- Производные ключи для каждого приложения (derived keys)
- Приватный ключ **никогда** не покидает устройство

**Где:** Level 1 (Micro-Kernel, Key Manager). Ключи хранятся на TPM/Secure Enclave при наличии.

### 1.1.1 Авторизация нового устройства (QR + ZKP)

**Процесс добавления устройства в mesh:**

1. Пользователь открывает CORE на уже авторизованном устройстве → "Добавить устройство"
2. Система генерирует QR-код: содержит публичный ключ нового устройства + одноразовый токен (TTL 10 мин)
3. Новое устройство сканирует QR (или вводит код вручную)
4. Обмен публичными ключами (Ed25519) через защищённый канал
5. Zero-Knowledge Proof: новое устройство доказывает владение секретом, не раскрывая его. Разработчики CORE не знают пароль пользователя.
6. Бэк добавляет device key в mesh-реестр. WireGuard-туннель устанавливается автоматически.

**Где:** Level 1 (Key Manager, Profile Manager) + Level 2 (Mesh Engine) + Level 0 (Host Shim, камера для QR).

### 1.2 SSH (Core.Hardcore)

- Только по ключевым парам
- Парольная аутентификация отключена по умолчанию
- Owner добавляет публичный ключ SSH при настройке

**Где:** Level 0 (Host Shim, SSH-сервер) + Level 1 (Key Manager).

### 1.3 Техподдержка

Бэк **глух** пока пользователь сам не инициирует техподдержку. Без инициативы пользователя — порт закрыт, запросы не принимаются, спам невозможен.

**Три способа вызвать:**
1. Голос: "техподдержка" → Intent Parser → экран техподдержки
2. Command Bar: набрал "техподдержка" → экран техподдержки
3. Телефонный звонок: техподдержка просит продиктовать код → пользователь открывает CORE → видит код

**Процесс:**
1. Пользователь говорит "техподдержка" или набирает в Command Bar
2. Бэк **открывает окно приёма** (TTL 10 мин) и генерирует код: `SUPPORT-9284-6173` (8 цифр)
3. Пользователь диктует код техподдержке по телефону
4. Техподдержка вводит код → relay-сервер CORE Corp пробивает NAT → WireGuard-туннель до Бэка
5. Бэк проверяет: окно открыто? код валидный? подпись CORE Corp верна?
6. На экране пользователя: "Подтверждённый специалист CORE Corp. Разрешить?" → одна кнопка
7. Пользователь подтверждает → сессия открыта (TTL 30 мин)
8. Пользователь видит "Специалист подключён" → может нажать "Отключить" в любой момент
9. Все действия логируются в аудит (категория "system")
10. Через 10 мин окно приёма закрывается → Бэк снова глух

**Relay-сервер CORE Corp:**
- Пробивает NAT — техподдержка может подключиться к любому Бэку за роутером
- Не хранит данные пользователей — только живые сессии (TTL 10 мин), потом удаляет
- Никакой регистрации устройств — Бэк сам подключается к relay при запросе техподдержки

**Подпись запросов:**
- Приватный ключ CORE Corp хранится в HSM (Hardware Security Module) — никогда не покидает железо
- Каждый запрос техподдержки подписан этим ключом (Ed25519)
- Бэк проверяет подпись публичным ключом CORE Corp (зашит в бинарник)
- Без валидной подписи — подключение невозможно, даже с правильным кодом

**Защита от атак:**
- Спам запросами — невозможен. Без валидного support-кода Бэк отвергает запрос, пользователь ничего не видит
- Перехват кода — код одноразовый, 100 млн комбинаций, TTL 10 мин
- Фишинг — пользователь сам инициирует. Без его действия окно приёма закрыто
- Подмена специалиста — подпись CORE Corp (HSM), подделать нельзя
- Взлом машины техподдержки — ключ в HSM, не извлекаем

**Уровни доступа:**

| Уровень | Что может | По умолчанию |
|---------|-----------|--------------|
| Read-only | Смотреть логи, конфиг, статус компонентов | Да |
| Diagnose | Read-only + запуск диагностических команд | По запросу |
| Full | Полный доступ (как Owner) | Только с явного согласия |

**Продвинутый режим (для гиков и админов):**
- SSH-доступ: `ssh support@back.address --support-code SUPPORT-9284-6173`
- Те же правила: код одноразовый, подпись CORE Corp, подтверждение Owner
- Whitelist IP для постоянной техподдержки (TTL на ключи, автоотзыв)
- Rate limiting: максимум 3 попытки с одного адреса, потом блокировка 30 мин

---

## 2. Шифрование

### 2.1 Три уровня безопасности

Owner выбирает уровень. Алгоритмы под капотом — система решает сама, пользователь их не видит.

| Уровень | Данные в покое | Ключи | Для кого |
|---------|---------------|-------|----------|
| Базовый | Шифрование диска, ключ на сервере. WireGuard для транзита. Защита от кражи диска | На сервере | Домашний Бэк, простые случаи |
| Повышенный | Zero-knowledge. Master key derived из биометрии/PIN устройства, сервер хранит только шифротекст. При компрометации сервера — данные бесполезны | Derived из TPM/Secure Enclave при входе; key material никогда не покидает устройство | Параноики, чувствительные данные |
| Максимальный | Повышенный + аппаратный ключ (YubiKey/TPM). Master key не покидает железо | YubiKey/TPM, никогда не покидают железо | Корпоративный, промышленный |

**Смена уровня:** Owner может переключить уровень в любой момент. При повышении — полная перешифровка всех данных.

### 2.2 Алгоритмы

Алгоритмы скрыты от пользователя, не настраиваются:

| Назначение | Алгоритм |
|-----------|----------|
| Данные в покое | AES-256-GCM |
| Ключевые пары | Ed25519 |
| Транзит | WireGuard (Noise protocol, ChaCha20-Poly1305) |
| Хеширование | BLAKE3 |

### 2.3 Key Manager — технически

**Что:** Система хранит ключи сама. Пользователь не вводит пароли.

```
Первый вход:
  +-- Генерация master key pair (Ed25519)
  +-- Master key зашифрован паролем/биометрией устройства
  +-- Производные ключи для каждого приложения (derived keys)
  +-- Приватный ключ НИКОГДА не покидает устройство

Авторизация приложений:
  +-- Приложение запрашивает доступ к сервису
  +-- Key Manager подписывает запрос (zero-knowledge proof)
  +-- Сервис отдаёт токен приложению
  +-- Приложение работает с токеном, не видя ключ
```

**Где:** Level 1 (Micro-Kernel, Key Manager).

### 2.4 Auth Proxy — технически

**Что:** Приложение никогда не видит пароль. Система делает запрос за пользователя.

```
Приложение: "Мне нужен доступ к GitHub"
  |
  v
Auth Proxy (Level 1):
  +-- Проверяет: есть ли разрешение в capabilities?
  +-- Если да -> спрашивает пользователя: "Разрешить?"
  +-- Если да -> OAuth flow через системный браузер
  +-- Результат: токен сохранён в Key Manager
  +-- Приложение получает только проксированный доступ
  +-- Приложение НЕ видит токен, НЕ видит пароль
```

**Где:** Level 1 (Micro-Kernel, Auth Proxy + Key Manager).

---

## 3. Авторизация (RBAC)

### 3.1 Роли и права

С точки зрения RBAC, Space = Бэк. Подключение к Space = подключение к Бэку. Доступ к данным и конфигурации определяется ролью на Бэке.

**Три встроенные роли:**

| Роль | Что может |
|------|-----------|
| Owner | Полный контроль. Всё в Space + конфигурация Бэка (политики, компоненты, бэкапы, пользователи, группы). Как root |
| Member | Чтение, запись, создание проектов, запуск приложений, работа в Space |
| Guest | Только чтение, запуск приложений, просмотр проектов |

Owner может создавать кастомные роли с произвольным набором capabilities и группы пользователей.

**Уровни назначения ролей:**

| Уровень | На что влияет |
|---------|--------------|
| Space | Доступ ко всему Space (все проекты, все файлы) |
| Проект | Доступ к конкретному проекту |

**Наследование:**
- Роль на Space = default для всех проектов внутри
- Роль на проекте переопределяет роль Space (только в сторону ограничения — Member на Space можно понизить до Guest на проекте, но не повысить Guest до Member)
- Персональная роль > групповой роли

**Безопасность:** контекстное меню — не Core.Backoffice. Это точечная операция на конкретный объект, доступная только Owner. Каждое изменение роли логируется Бэком (аудит).

### 3.2 Управление ролями — UI

- Контекстное меню — Owner кликнул на проект/Space → "Права доступа" → назначил роль
- Command Bar — быстрое назначение: `@ivan → роль member на проект "ЖК"`
- Core.Hardcore — массовые операции, группы, создание пользователей

### 3.3 RBAC Engine — технически

```
RBAC Engine (Level 1, Micro-Kernel):
  |
  +-- Role Store (SQLite):
  |   +-- roles: id, name, type (builtin|custom), capabilities[]
  |   +-- Builtin:
  |   |   +-- Owner: all capabilities + управление сервером
  |   |   +-- Member: read, write, create, execute
  |   |   +-- Guest: read, execute
  |   +-- Custom: Owner создаёт с произвольным набором capabilities
  |
  +-- Assignment Store (SQLite):
  |   +-- user_id, role_id, scope_type (space|project), scope_id
  |   +-- Пример: ivan → Owner → space:работа
  |   +-- Пример: elena → Member → space:работа, Guest → project:бюджет
  |
  +-- Group Store (SQLite):
  |   +-- groups: id, name
  |   +-- group_members: group_id, user_id
  |   +-- group_roles: group_id, role_id, scope_type, scope_id
  |
  +-- Проверка при каждом API-запросе:
      +-- Запрос от Фронта → RBAC Engine
      +-- Проверка: user → role на scope → capability
      +-- OK → выполнение, Отказ → ошибка
      +-- Наследование: роль проекта ограничивает роль Space
      +-- Приоритет: персональная роль > групповой роли
```

**Интерфейсы управления:**

```
Контекстное меню (Level 3):
  +-- Owner кликнул на проект/Space → "Права доступа"
  +-- Список пользователей/групп с ролями
  +-- Назначение/изменение роли → API-запрос в Бэк

Command Bar (Level 1):
  +-- "@ivan → роль member на проект 'ЖК'"
  +-- Input Router → Intent Parser → RBAC Engine

Core.Hardcore (SSH, CLI):
    +-- core-cli role assign --user ivan --role member --scope project:ЖК
    +-- core-cli group create --name "Отдел дизайна"
    +-- core-cli group add --group "Отдел дизайна" --user ivan
```

**Где:** Level 1 (RBAC Engine, SQLite) + Level 3 (контекстное меню).

---

## 4. Аудит

### 4.1 Что логируется — 13 категорий

Бэк логирует действия пользователей. Owner = root, полностью контролирует настройки аудита. Хочет отключить — отключает.

**Авторизация и устройства:**
- Вход в Space (успешный, неуспешный)
- Выход из Space
- Подключение устройства (IP, тип устройства, ОС)
- Отключение устройства
- Потеря связи / восстановление связи
- Генерация кода приглашения
- Использование кода приглашения (кто подключился)
- Отзыв доступа устройству

**Роли и группы:**
- Назначение / изменение роли пользователю
- Создание / удаление кастомной роли
- Создание / удаление группы
- Добавление / удаление пользователя из группы

**Проекты:**
- Создание, открытие, закрытие, переименование, архивирование, удаление проекта
- Изменение настроек и layout проекта

**Файлы:**
- Создание, открытие, редактирование, переименование, перемещение, копирование файла
- Загрузка (upload), скачивание (download), удаление, восстановление файла

**Заметки и задачи:**
- Создание, редактирование, удаление заметки/задачи
- Изменение статуса задачи, назначение задачи на пользователя

**Теги:**
- Создание тега, назначение/удаление тега с сущности, удаление тега

**Мессенджер и почта:**
- Отправка, чтение, удаление сообщения/письма
- Входящий звонок (принят/отклонён/пропущен), исходящий звонок (длительность)
- Поиск по переписке

**Поиск:**
- Поисковый запрос в Command Bar (текст запроса)
- Поиск по файлам, тегам, переписке (запрос + результаты)

**Приложения:**
- Запуск, закрытие приложения (какое, в каком проекте, длительность)
- Установка, удаление, обновление приложения из App Registry
- Запрос приложением API (endpoint, результат)

**Браузер:**
- Открытие URL, закрытие вкладки/окна
- Поисковый запрос в веб-окне
- Время на странице

**Профили:**
- Создание, переключение, удаление профиля
- Анонимный профиль (создание/закрытие)

**Система (только Owner):**
- Изменение конфигурации Бэка (компоненты, политики, аудит)
- Бэкап (создание, восстановление)
- Обновление, остановка, перезапуск Бэка

**Space (мульти-Бэк):**
- Подключение/отключение от другого Бэка
- Копирование данных между Space (попытка, результат)
- Изменение политики мульти-Бэка

### 4.2 Настройки (Owner контролирует)

| Настройка | Что |
|-----------|-----|
| Логирование | Вкл / Выкл |
| Категории | Чекбоксы для каждой из 13 категорий — Owner включает нужные |
| Логировать | Пользователи / Группы / Приложения (пусто = все) |
| Исключения | Пользователи / Группы / Приложения (исключение побеждает include) |
| Срок хранения | Произвольный (`7d`, `12h`, `30m`). Пусто или `0` = бесконечно |
| При переполнении | Архивировать / Перезаписывать старые |

`include` пустой = логируются все. `exclude` побеждает `include` — исключённый пользователь/группа/приложение не логируется, даже если попадает под include.

**Где хранится:** отдельная SQLite-таблица в Бэке, не смешивается с пользовательскими данными.

**Кто видит:** Owner — всё. Member/Guest — не видят логи.

**Где смотреть:**
- Core.Hardcore — полный доступ, фильтрация, экспорт: `core-cli audit log --user ivan --from 2025-01-01 --category deletion`
- Command Bar — Owner: "кто удалял файлы за последнюю неделю"
- Контекстное меню — правый клик на файл/проект → "История доступа"

### 4.3 Audit Engine — технически

```
Audit Engine (Level 1, Micro-Kernel):
  |
  +-- Audit Store (SQLite, отдельная таблица):
  |   +-- log: id, timestamp, user_id, action, target_type, target_id, result, device_id, details
  |
  +-- 13 категорий:
  |   +-- auth, roles, projects, files, notes_tasks, tags
  |   +-- messenger, search, apps, browser, profiles
  |   +-- system, multiback
  |
  +-- Конфигурация (JSON, SQLite):
  |   {
  |     "audit": {
  |       "enabled": true,
  |       "retention": "90d",
  |       "overflow": "archive",
  |       "categories": { ... },
  |       "include": { "users": [], "groups": [], "apps": [] },
  |       "exclude": { "users": [], "groups": [], "apps": [] }
  |     }
  |   }
  |
  +-- При каждом API-запросе:
      +-- Audit Engine проверяет: включена ли категория?
      +-- Проверяет include/exclude фильтры (exclude побеждает)
      +-- Записывает в Audit Store
      +-- Ротация по retention (async, background)
```

**Где:** Level 1 (Audit Engine, SQLite). Просмотр: Level 1 (CLI) + Level 3 (UI) + Level 4 (Intent API).

---

## 5. User Directory — провайдеры пользователей

### 5.1 Провайдеры

Бэк не привязан к конкретному источнику пользователей. Owner выбирает провайдер через абстракцию `UserDirectory`:

```
UserDirectory
├── LocalDirectory    — встроенный, пользователи создаются вручную (по умолчанию)
├── LDAPDirectory     — Active Directory, FreeIPA, OpenLDAP
├── OAuthDirectory    — Google Workspace, Microsoft 365, GitHub
└── CustomDirectory   — плагин для любых кастомных провайдеров
```

**Как работает:**
1. Owner при настройке Бэка выбирает провайдер: Local / LDAP / OAuth / Custom
2. Настраивает подключение (адрес сервера, bind DN для LDAP; client ID/secret для OAuth)
3. Бэк синхронизирует пользователей и группы автоматически
4. Маппинг групп провайдера → роли CORE: Owner задаёт правила ("группа 'Domain Admins' → Owner, 'Domain Users' → Member")
5. Аутентификация проходит через провайдер — CORE не хранит пароли пользователей

**Домашний Бэк:** LocalDirectory — никаких внешних провайдеров, пользователи создаются вручную или по приглашению.

### 5.2 UserDirectory — технически

```
UserDirectory Interface (Level 1):
  |
  +-- LocalDirectory (default):
  |   +-- Пользователи создаются вручную или по приглашению
  |   +-- Хранение: SQLite
  |
  +-- LDAPDirectory:
  |   +-- AD, FreeIPA, OpenLDAP
  |   +-- Настройка: server, bind_dn, base_dn
  |   +-- Sync: периодическая синхронизация пользователей и групп
  |   +-- Auth: bind-запрос к LDAP-серверу
  |
  +-- OAuthDirectory:
  |   +-- Google Workspace, Microsoft 365, GitHub
  |   +-- Настройка: client_id, client_secret, discovery_url
  |   +-- Auth: OAuth flow через системный браузер
  |
  +-- CustomDirectory:
      +-- Плагин для кастомных провайдеров
      +-- Interface: sync(), auth(), getGroups()
  |
  +-- Маппинг групп → роли CORE:
      +-- directory_groups → role_id, scope_type, scope_id
      +-- Пример: LDAP "Domain Admins" → Owner на весь Space
```

**Настройка:**

```
Core.Hardcore:
  +-- core-cli directory set --type ldap --server ldap://ad.company.com ...
  +-- core-cli directory map --ldap-group "Domain Admins" --role owner
  +-- core-cli directory sync

Command Bar (Owner):
  +-- "Подключить LDAP: ad.company.com, маппинг: Admins → Owner"
```

**Где:** Level 1 (Micro-Kernel, UserDirectory interface + implementations).

---

## 6. Изоляция

### 6.1 Приложения (V8 Isolates)

V8 Isolate приложения не имеет прямого доступа к файловой системе, сети, контактам. Единственный канал получения данных — API-запрос в Бэк. Бэк проверяет права и либо отдаёт данные, либо отказывает.

Даже взломанное приложение не может прочитать чужие данные — потому что Фронт их просто не имеет. Данные лежат в Бэке, доступ контролирует Бэк.

**Модель безопасности по уровням приложений** (подробно: [Layer Apps, секция Безопасность](layer-6-apps.md)):

| Уровень | Sandbox | Доступ к данным | Риск |
|---------|---------|----------------|------|
| 1-2. «Как есть» / «Манифест» | Island Mode (Chromium) | Только свои cookies/storage | Как в браузере |
| 3. «Бэк на месте» | Island Mode + V8 Isolate | App-scoped SQLite + VFS | Изолированный бэкенд |
| 4. «@core/*» | Island Mode | Через @core/* (capability) | Только разрешённое |
| 5. «Полный натив» | V8 Isolate (Bun) | Через fs shim (capability) | Только разрешённое |

**Permissions (уровни 2-5):** приложения запрашивают в манифесте, пользователь подтверждает. `notifications`, `fullscreen`, `fs`, `contacts`, `network`, `voice`, `graphics`. Для уровня 3 — sandboxed V8 Isolate, не нужен permissions (app-scoped SQLite + сеть).

**Аудит:** действия приложений логируются в категорию «apps» — запуск, закрытие, API-вызовы, запросы permissions.

**Capability-based Security:**

```typescript
const context = {
  fs: {
    read: ["/project/abc/**"],
    write: ["/project/abc/**"],
  },
  network: {
    domains: ["api.example.com"],
  },
  graphics: true,
  mind: true,
  contacts: false,
};
```

**Защита от побега из sandbox:** V8 Isolate + seccomp + отдельный процесс. Минимальные системные вызовы.

**Где:** Level 1 (Micro-Kernel, Capability Security).

### 6.2 Space (изоляция между Бэками)

Окно из одного Space не может обратиться к данным другого Space. V8 Isolate привязан к конкретному Space — все API-вызовы маршрутизируются только в его Бэк.

```
V8 Isolate (Space: Работа)
  ├── api.fs.read("договор.docx")  → Корпоративный Бэк — OK
  ├── api.fs.read("фото_отпуск")   → Корпоративный Бэк — "Файл не найден"
  └── api.network.request("домашний_бэк")  → заблокировано runtime
```

**Политики Бэка:**

Каждый Бэк определяет свои политики. Не решение Фронта — решение владельца Бэка.

**Корпоративный Бэк может:**
- Запретить подключение к другим Бэкам одновременно
- Разрешить, но запретить перетаскивание данных между Space
- Разрешить всё

**Домашний Бэк:** не ограничивает ничего.

Политики передаются Фронту при подключении. Фронт обязан выполнять. При конфликте политик — **побеждает более строгая**. Если хоть один подключённый Бэк запрещает мульти-Бэк — Фронт работает только с ним.

### 6.3 Clipboard (политики экспорта)

**Пользователь настраивает (комфорт):**

| Настройка | По умолчанию |
|-----------|-------------|
| Размер стека | 20 |
| Типы контента | Все (текст, изображения, файлы, ссылки) |
| Очистка при выходе из Space | Нет |

**Owner настраивает через политику Бэка (безопасность):**

| Настройка | Что |
|-----------|-----|
| `clipboard_allow_export` | `none` / `text` / `all` — что можно выносить в другой Space |
| `clipboard_life_time` | Максимальный срок хранения в буфере |
| `clipboard_persist` | Разрешить ли сохранять буфер между сессиями |

**При вставке:** Фронт проверяет политику Бэка-источника:
- `none` — "Политика запрещает копирование из этого Space"
- `text` + пользователь копирует файл — "Используйте 'Скопировать в Space' для передачи файлов"
- `all` — всё разрешено

**По умолчанию (global):** текст копируется свободно между Space. Файлы, изображения, контакты — через механизм копирования между Space.

**Корпоративный шаблон (default для новых корпоративных Space):** `clipboard_allow_export: none`. Owner корпоративного Бэка может переключить на `text` или `all`.

### 6.4 Копирование данных между Space

Бэк-контролируемая операция. Фронт — только триггер, не видит содержимое копируемых данных.

**Процесс:**
1. Пользователь копирует файл из Space A в Space B (drag-and-drop, Command Bar, контекстное меню)
2. Фронт отправляет запрос в Бэк A: "экспорт файла X для Space B"
3. Бэк A проверяет политику → разрешает/отказывает
4. Если разрешено — Бэк A генерирует временный токен экспорта (зашифрованная ссылка на данные, короткий TTL)
5. Фронт передаёт токен в Бэк B: "импорт файла по токену"
6. Бэк B проверяет свою политику → разрешает/отказывает
7. Если разрешено — Бэк B связывается с Бэком A напрямую (P2P), передаёт токен, получает данные
8. Данные передаются **между Бэками напрямую** — Фронт не участвует в передаче

**Если Бэки на одном устройстве (IPC):** Бэк A → IPC → Бэк B. Фронт вообще не участвует.

**Ограничения:**
- Бэк A может ограничить, что можно экспортировать (только файлы, не контакты; только определённые проекты)
- Бэк B может ограничить, что можно импортировать (только определённые типы файлов)
- Каждый Бэк логирует операцию в свой аудит-лог (если категория "мульти-Бэк" включена)

---

## 7. Векторы атак и защита

### 7.1 Взлом Бэка (сервера)

| Вектор атаки | Защита |
|-------------|--------|
| Эксплойт в коде CORE | Регулярные обновления, автоматический бэкап, откат при ошибке |
| Взлом ОС сервера | Минимальная ОС (только CORE), нет лишних сервисов, один открытый порт (WireGuard) |
| Физический доступ к серверу | Шифрование диска, BIOS-пароль, отключённые USB |
| Подмена обновления | Подпись пакетов (Ed25519) + хеш (BLAKE3). Бэк проверяет перед установкой |
| Утечка ключей шифрования | Ключи в RAM (не на диске) при уровне "Повышенный". На TPM/YubiKey при "Максимальный" |

**Минимальная поверхность атаки:**
- Один открытый порт — WireGuard
- SSH — только по ключам, пароли отключены
- Один пользователь ОС — `core`
- Процесс изолирован: seccomp, chroot, отдельный пользователь
- Файловая система — read-only где возможно

### 7.2 Подключение к чужому Space

| Вектор атаки | Защита |
|-------------|--------|
| Перехват кода приглашения | Код одноразовый, TTL 24ч, уничтожается после использования |
| Подмена ключей при handshake | ECDH — взаимная аутентификация, обе стороны проверяют публичные ключи |
| MITM при подключении | WireGuard-туннель — шифрование всего трафика |
| Повторное использование кода | Код уничтожается после первого использования |

### 7.3 Приложения (V8 Isolates)

| Вектор атаки | Защита |
|-------------|--------|
| Взломанное приложение | V8 Isolate — изоляция памяти. Нет доступа к диску, сети, контактам напрямую |
| Запрос чужих данных | Все запросы через API Бэка. Бэк проверяет capabilities + RBAC-роль |
| Побег из sandbox | V8 Isolate + seccomp + отдельный процесс. Минимальные системные вызовы |

### 7.4 Мульти-Бэк (между Space)

| Вектор атаки | Защита |
|-------------|--------|
| Утечка данных между Space | V8 Isolate привязан к конкретному Space. Не может обращаться к другому |
| Копирование данных без разрешения | Каждый Бэк проверяет свою политику. Оба должны разрешить |
| Буфер обмена между Space | `clipboard_allow_export`: none/text/all — настраивает Owner |
| Конфликт политик | Побеждает более строгая |

### 7.5 Синхронизация (P2P/CRDT)

| Вектор атаки | Защита |
|-------------|--------|
| Перехват трафика | WireGuard — всё зашифровано |
| Подмена данных в транзите | CRDT — каждая операция подписана ключом устройства |
| Подключение чужого устройства | Device key (Ed25519) — без ключа подключиться нельзя |

#### Seeding и смена primary Бэка

| Угроза | Защита |
|--------|--------|
| Поддельный target (злоумышленник притворяется новым Бэком) | Device key pinning: target должен доказать владение device key, зарегистрированным в mesh. При ручном добавлении — код приглашения от Owner |
| Несанкционированная инициация переноса | Только Owner (или админ с правом `back:promote`) может инициировать seeding. RBAC-проверка на source |
| Перехват snapshot | WireGuard (ChaCha20-Poly1305) + ECDH handshake. Snapshot подписан source device key (Ed25519), target проверяет подпись перед активацией |
| Подмена snapshot в пути | Merkle root validation + подпись snapshot. Любая модификация нарушит подпись или Merkle root |
| DoS через массовый seeding | Rate limiting: один seeding в 24 часа на source. Повторный seeding требует явного подтверждения Owner |
| Утечка данных со старого Бэка | Source переходит в cached mode — данные остаются для fallback. Для вывода из эксплуатации — команда `secure wipe` с перезаписью |
| Аудит | Операция логируется: кто, когда, source → target, объём, результат |
| Требование recovery | Для уровней «Повышенный» и «Максимальный» — ввод recovery-фразы или биометрия на source перед seeding'ом |

### 7.6 Внешние носители (USB, CD/DVD)

| Вектор атаки | Защита |
|-------------|--------|
| Вредоносный файл | Файлы импортируются в VFS, приложения не запускают их напрямую. Импортированный `.exe` не выполнится |
| Автозапуск с носителя | Нет автозапуска. Пользователь явно выбирает действие |
| Подмена носителя (BadUSB) | Host Shim определяет тип устройства. Предупреждение при несовпадении |
| Огромное количество файлов (DoS) | Лимиты: max файлов за импорт (default: 100 000), max размер файла (default: 10 ГБ) |
| Кража данных через экспорт | Политика `external_media_allow_export` (USB и др.). Owner может запретить |

Подробно: [Layer Devices, секция 7](layer-5-devices.md).

### 7.7 Сетевые ресурсы (SMB/NFS)

| Вектор атаки | Защита |
|-------------|--------|
| Вредоносный файл на сетевой шаре | Та же защита что с USB — файлы импортируются в VFS, не выполняются напрямую |
| Несанкционированный доступ к шаре | Аутентификация — через хост-ОС (SMB-пароль/Kerberos). Workspace не хранит учётные данные |
| Перехват трафика SMB | Хост-ОС отвечает за шифрование (SMB3 encryption). Workspace не контролирует этот уровень |
| Подмена сетевого ресурса (MITM) | За пределами Workspace — защита на уровне хост-ОС и сетевой инфраструктуры |

Подробно: [Layer Devices, секция 10.1](layer-5-devices.md).

---

## 8. Обновления — безопасность

### 8.1 Подпись пакетов

- Пакеты подписаны Ed25519 (ключ CORE Corp в HSM)
- Бэк проверяет подпись + хеш (BLAKE3) перед установкой
- Приватный ключ CORE Corp хранится в HSM — никогда не покидает железо
- Без валидной подписи — установка невозможна

### 8.2 Процесс обновления

**Бэк:**
- Обновляется с подтверждением Owner
- Перед обновлением — автоматический бэкап
- При ошибке — откат к предыдущей версии
- Owner может отложить обновление

**Фронт:**
- Обновляется автоматически и бесшумно
- Backward compatible с предыдущей версией Бэка
- Layout, проекты, кэш не ломаются при обновлении

---

## 9. Кто что контролирует

| Что | Пользователь | Owner (Бэка) | CORE Corp |
|-----|-------------|-------------|-----------|
| Свои настройки Фронта | Да | — | — |
| Приоритеты уведомлений | Да | — | — |
| Настройки мультибуфера (комфорт) | Да | — | — |
| Вызов техподдержки | Да | — | — |
| Подтверждение подключения техподдержки | Да | — | — |
| Роли и группы | — | Да | — |
| Политики Бэка (clipboard, audit, etc) | — | Да | — |
| Уровень безопасности | — | Да | — |
| User Directory | — | Да | — |
| Профиль Бэка (компоненты) | — | Да | — |
| Подпись обновлений | — | — | Да (HSM) |
| Подпись запросов техподдержки | — | — | Да (HSM) |
| Relay-сервер для техподдержки | — | — | Да |

---

## 10. Уровень 3: защита от эксфильтрации

**Проблема:** Уровень 3 («Бэк на месте») получает sandboxed V8 Isolate с app-scoped SQLite и ограниченный сетевой доступ через whitelist доменов в манифесте. Однако если whitelist широкий или не настроен, приложение может выкачать данные из SQLite и отправить на внешний сервер. Другие приложения (уровень 1–2, Island Mode) могут обратиться к `localhost:PORT` бэкенда уровня 3 и через его API выкачать данные.

**Решения:**

- **Whitelist доменов** в манифесте (`core.json`): уровень 3 указывает, к каким доменам может обращаться. Не указан — доступ запрещён.
- **Firewall-правило:** только frontend Island этого приложения может обращаться к `localhost:PORT` бэкенда уровня 3. Другие приложения не могут стучаться на чужой локальный порт.
- **Network audit:** Бэк логирует все исходящие HTTP-запросы уровня 3 в аудит (категория «apps»). Owner видит, куда приложение стучится.

---

## 11. Доверие к хост-ОС

**Проблема:** Workspace работает поверх хост-ОС и полностью ей доверяет. Компрометация хост-ОС (rootkit, malware) = обход всех гарантий Workspace. Хост-ОС может читать память, перехватывать IPC, модифицировать файлы VFS.

**Решения:**

- **Явная документация риска:** trusted computing base includes host OS. Пользователь информирован.
- **Integrity Monitoring Agent (опционально):** периодически проверяет хеш бинарника Workspace. При изменении — предупреждение Owner.
- **Remote attestation (опционально):** Бэк может запросить у Фронта proof-of-integrity (TPM-based). Для корпоративных сценариев.
- **Dedicated device:** для чувствительных данных — Core Base (выделенное железо, минимальная поверхность атаки).

---

## 12. Sandbox и аудит кода приложений

### 12.1 V8 Bytecode — аудит невозможен

**Проблема:** Приложения уровней 3–5 могут распространяться как bytecode. Никто не может проверить, что код делает. Сочетание «невозможность аудита + свободная сеть» = идеальный вектор эксфильтрации.

**Решения:**

- **Runtime monitoring:** все вызовы `@core/*` API и сетевые запросы перехватываются и логируются.
- **Политика `allow_bytecode_apps`:** корпоративный Owner может запретить bytecode-приложения (`false` по умолчанию для корпоративных Бэков).
- **Магазин pkg.core.app** принимает только исходный код; bytecode создаётся локально при установке.
- **Behavioural analysis:** выявление аномальных паттернов (массовые чтения из SQLite + сетевые запросы).

### 12.2 Недостаточная sandbox-изоляция

**Проблема:** Указано «V8 Isolate + seccomp + отдельный процесс», но нет конкретики: какой seccomp-профиль? какие syscall разрешены? Нет упоминания namespaces, V8 Sandbox, процедур при побеге.

**Решения:**

- **Полный контейнерный sandbox:** seccomp-bpf + user/mount/PID/net namespaces
- **V8 Sandbox:** внедрён memory corruption mitigation (v8.dev/blog/sandbox)
- **Каждый V8 Isolate — отдельный процесс** с минимальным PID-namespace
- **Runtime integrity checks:** периодическая верификация памяти Isolate

---

## 13. Буфер обмена между Space

**Проблема:** По умолчанию текст копируется между Space свободно. «Текст» может содержать IP-адреса, токены, пароли, финансовые данные. В корпоративном сценарии сотрудник может скопировать текст из корпоративного Space и вставить в личный.

**Решения:**

- Для корпоративных Space: default `clipboard_allow_export: none` (не `text`)
- Опция «подтверждение при вставке между Space»
- Для корпоративных Space — обязательная очистка буфера при переключении Space

---

## 14. P2P/CRDT: rate limiting и откат

**Проблема:** CRDT гарантирует отсутствие конфликтов, но не защищает от вредоносных операций. Компрометированный узел может генерировать миллионы операций в секунду (DoS). Нет механизма отмены операций.

**Решения:**

- **Rate limiting:** max N операций в секунду от одного устройства
- **CRDT garbage collection:** операции старше M дней, уже merged — удаляются
- **CRDT checkpoint:** периодический снапшот состояния для отката
- **CRDT quarantine:** подозрительный поток от узла приостанавливается до подтверждения Owner

---

## 14.5 Dark-Mesh (Протокол Омега)

**Проблема:** Попытка блокировки на уровне магистральных провайдеров или государства. Сетевой трафик CORE идентифицируется и фильтруется.

**Решение — План "Б":**

- **Активация:** автоматически (при обнаружении блокировки) или вручную (Owner активирует)
- **Обфускация:** трафик маскируется под обычный HTTPS-шум или видеозвонки (WebRTC-стеганография)
- **Протокол Омега:** узлы скрываются за обфусцированными соединениями, неотличимыми от легитимного трафика
- **Резистентность:** даже если провайдер видит поток — он не может отличить CORE от обычного видеочата

**Ограничения:** Небольшое снижение пропускной способности (~15%) из-за обфускации. Включается только при необходимости.

**Где:** Level 0 (Host Shim, network stack) + Level 2 (Mesh Engine).

---

## 15. App Registry: модерация и отзыв

**Проблема:** Приложения попадают на устройство из нескольких источников без верификации содержания. Нет механизма отзыва вредоносных приложений (kill switch), нет certificate pinning, нет анализа подозрительных комбинаций permissions.

**Решения:**

- **Mandatory code review + automated security scan** для pkg.core.app
- **Remote app kill:** CORE Corp может пометить приложение как вредоносное, все Бэки автоматически удаляют
- **Certificate pinning** для pkg.core.app и update_url
- **Анализ suspicious permission combinations:** `fs` + `network` + `contacts` = высокий риск

---

## 16. Мультипользовательность: изоляция памяти

**Проблема:** Оба профиля работают в одном процессе Фронта. При freeze/thaw — V8 Isolates уничтожаются, но память может не быть обнулённой. Данные могут оставаться в SharedArrayBuffer. Не описано, что происходит с clipboard при переключении профилей.

**Решения:**

- **Secure memory wipe** при freeze профиля: обнулять все V8 heap regions
- **Clipboard автоматически очищается** при переключении профиля
- **Каждый профиль — отдельный SharedArrayBuffer region**

---

## 17. Шифрование диска

**Проблема:** Для Core Base и Raspberry Pi нет упоминания encrypted rootfs. Если вытащить SD-карту — всё на ней. Нет защиты при физическом подключении клавиатуры/монитора к Core Base.

**Решения:**

- **Encrypted rootfs по умолчанию** для Core Base и Raspberry Pi (LUKS / BitLocker)
- **Boot-time passphrase** через приложение CORE на телефоне (по QR)
- При подключении монитора/клавиатуры к Core Base — предупреждение Owner + аудит

---

## 18. Средние угрозы

### 18.1 Фронт-кэш на украденном устройстве

**Проблема:** При краже работающего устройства кэш Фронта доступен в открытом виде.

**Решения:**
- Шифровать кэш Фронта ключом от device key + screen lock
- При простое > N минут — шифровать кэш
- Remote wipe: Owner может удалённо затребовать уничтожение кэша

### 18.2 core-dev — вектор произвольного кода

**Проблема:** `core-dev install` принимает URL любого Git-репозитория. `--dev` даёт доступ к DevTools без ограничений.

**Решения:**
- core-dev может включить только Owner
- Для корпоративных Бэков — whitelist репозиториев
- Логи core-dev показывают только API-вызовы своих приложений и попадают в аудит

### 18.3 Техподдержка — инсайдерская угроза

**Проблема:** Сотрудник CORE Corp с доступом к HSM — единственная точка отказа. Уровень «Full» даёт полный доступ как Owner за 30 минут без dual approval.

**Решения:**
- Dual approval для уровня Full
- Все сессии уровня Full — обязательная видеозапись
- HSM key ceremony: минимум 2 из 3 сотрудников для подписи
- Anomaly detection: если техподдержка обращается к одному Бэку > N раз — alert

### 18.4 Обновления — компрометация ключа разработчика

**Проблема:** Приватный ключ разработчика может быть скомпрометирован. Нет certificate pinning для update_url.

**Решения:**
- Registry of developer keys (CORE Corp может отозвать)
- Mandatory HTTPS для update_url
- Certificate pinning при первой установке
- `core-dev rollback` — только для Owner

### 18.5 Голос — всегда включённый микрофон

**Проблема:** Микрофон всегда активен. Нет аппаратного индикатора записи.

**Решения:**
- Аппаратный индикатор записи (LED)
- Аудио-буфер обнуляется после распознавания
- Пользователь может полностью отключить микрофон
- Integrity check Whisper model (BLAKE3)

### 18.6 Owner может отключить аудит

**Проблема:** Owner = root, может отключить аудит, выполнить вредоносные действия, включить обратно. Нет immutable audit trail.

**Решения:**
- Immutable audit store (append-only WAL, Owner не может удалить записи)
- Опциональный external audit: отправка на syslog/SIEM
- Для корпоративного профиля: аудит невозможно полностью отключить

### 18.7 SMB/NFS — учётные данные через хост-ОС

**Проблема:** Пароли SMB могут сохраняться в credential store хост-ОС. Host Shim вызывает `mount -t cifs` с паролем в командной строке — пароль виден в /proc.

**Решения:**
- Не сохранять SMB-пароли в credential store хост-ОС
- Для Linux: credentials file, не command-line
- Предупреждение: «Данные на сетевой папке не защищены Workspace»

### 18.8 Recovery-фраза — единственная точка отказа

**Проблема:** Recovery-фраза (24 слова BIP-39) — единственный способ восстановить данные. Украдена + stolen backup = полный доступ. Утеряна = невозвратимая потеря.

**Решения:**
- Shamir's Secret Sharing: 24 слова делятся на 3 комплекта, нужно 2 для восстановления
- Опция «social recovery» — доверенные контакты хранят части фразы
- Для корпоративного: HSM-based recovery

### 18.9 Error.message — целенаправленная эксфильтрация

**Проблема:** Злоумышленное приложение может генерировать ошибки с пользовательскими данными в message. Rate limiting (10/час) ограничивает, но не исключает.

**Решения:**
- Mandatory HTTPS для error_reporting_url
- Rate limit уменьшен до 3/час
- Пользователь видит preview полного отчёта перед отправкой

### 18.10 CRDT-журнал на украденном устройстве

**Проблема:** CRDT-журнал хранится локально. Не описано: зашифрован ли? При краже — история всех операций может быть прочитана.

**Решения:**
- CRDT-журнал шифруется device key + session key
- При отзыве device key — журнал нечитаем
- Pending мутации от отозванного устройства — Бэк отклоняет

### 18.11 Анонимный профиль — данные в swap

**Проблема:** In-memory SQLite для анонимного профиля, но нет `mlock()/VirtualLock()`. Хост-ОС может сбросить страницу памяти на диск.

**Решения:**
- `mlock()/VirtualLock()` для памяти анонимного профиля
- Явное обнуление памяти перед освобождением
- Рекомендация: для максимальной анонимности отключите swap в хост-ОС

---

## 19. Противоречия безопасности

### 19.1 «Нет доступа к сети» vs «Свободная сеть» для уровня 3

`layer-3-system-split.md` заявляет: «V8 Isolate приложения не имеет прямого доступа к сети». Но `layer-6-apps.md` для уровня 3 указывает: «Сеть (HTTP/HTTPS) — свободно». Прямое противоречие.

**Решение:** Уровень 3 имеет ограниченный сетевой доступ через whitelist доменов в манифесте. Общий принцип Layer 3 верен для уровней 1–2, 4–5. Для уровня 3 — отдельная модель с контролем.

### 19.2 Уровень 3 менее безопасен, чем уровень 4

Уровень 3: сеть через whitelist в манифесте, app-scoped SQLite. Уровень 4: сеть только через `@core/network` (capability). Более глубоко интегрированное приложение имеет **больше свободы** и **меньше контроля**. Нумерация уровней предполагает возрастание интеграции, но безопасность не возрастает монотонно.

**Решение:** Явно документировано, что уровень 3 — отдельная модель безопасности с повышенным риском, компенсируемым network audit и whitelist.

### 19.3 Auth Proxy vs. уровень 3

Auth Proxy описан как системный механизм, но уровень 3 с прямым сетевым доступом может обойти его, обратившись к внешнему сервису напрямую и перехватив OAuth-токен.

**Решение:** Auth Proxy не применим к уровню 3 (бэкенд имеет собственный сетевой стек). OAuth flow для уровня 3 должен проходить через Бэк, а не через приложение.

### 19.4 Master key: «никогда не покидает устройство» vs. «исчезает при перезагрузке»

Если master key «исчезает при перезагрузке» (уровень «Повышенный»), как пользователь получает доступ к данным после перезагрузки?

**Решение:** Master key derived from biometrics/PIN stored in TPM/Secure Enclave. «Никогда не покидает устройство» верно. «Исчезает из RAM при перезагрузке» — уточнено: re-derived при входе.

---

## 20. Пробелы безопасности — не описано нигде

| # | Пробел | Что нужно | Предварительное решение |
|---|--------|-----------|------------------------|
| 20.1 | App Kill Switch | Механизм отзыва вредоносных приложений | Remote app kill (см. секция 15) |
| 20.2 | Key Rotation | Ротация ключей при компрометации | Owner инициирует rotation → все устройства переподключаются |
| 20.3 | Rate Limiting API | Лимит API-запросов от приложений | Max N запросов/сек от одного Isolate, логируется в аудит |
| 20.4 | Secure Deletion | Удаление файлов | По умолчанию — wipe (перезапись случайными данными), не только пометка |
| 20.5 | Memory Protection | `mlock` для максимального уровня безопасности | `mlock()/VirtualLock()` для sensitive data при уровне «Максимальный» |
| 20.6 | Clock Synchronization | Манипуляция системными часами хост-ОС | HLC (Hybrid Logical Clocks) не зависят от системного времени; монотонность проверяется |
| 20.7 | Screen Capture Protection | Скриншоты и эксфильтрация | Опциональная защита: blur/watermark для confidential Space; запрет скриншотов через хост-ОС |
| 20.8 | Session Management | TTL, auto-lock, принудительный logout | Auto-lock после N минут простоя; Owner может принудительно завершить сессию устройства |
| 20.9 | Supply Chain Attacks | Компрометация зависимостей | Pinning хешей зависимостей (Rust crates, Bun packages); reproducible builds |
| 20.10 | Incognito Mode | Island Mode в инкогнито | Отдельный Isolate, no cache, no cookies, DNS через DoH, auto-wipe при закрытии |
| 20.11 | Accessibility Attack Vectors | Screen readers читают чужие окна | Accessibility API доступен только для системных приложений; сторонние приложения изолированы |
| 20.12 | Remote Renderer Security | Шифрование bitmap-стрима | TLS для bitmap-стрима; защита от screen scraping на стороне Бэка |
| 20.13 | Boot Integrity | Проверка целостности бинарника | Integrity Monitoring Agent (опционально); signed bootloader для Core Base |
| 20.14 | Data Residency | Compliance (GDPR, 152-ФЗ) | Owner выбирает юрисдикцию Бэка; P2P-узлы не пересекают границы без согласия |
| 20.15 | Incident Response | Что делать при инциденте | Runbook: изоляция узла, отзыв ключей, аудит, уведомление Owner; интеграция с SIEM |
| 20.16 | Seeding Security | Полный протокол безопасности при переносе Бэка: аутентификация target, RBAC на инициацию, подпись snapshot, rate limiting, аудит | См. §7.5 Seeding |

---

## 21. Threat Model (STRIDE)

Формальная модель угроз по методологии **STRIDE** для всех ключевых компонентов системы.

**Дата:** 2026-05-09  
**Область:** Workspace — распределённая Web-native ОС (Фронт, Бэк, P2P, приложения).  
**Границы:** весь стек от Host Shim до Intent API. Хост-ОС вне зоны детального анализа (см. accepted risk в этот документ, секция 11).  
**Ссылки:** этот документ, [layer-6-apps.md](layer-6-apps.md).

---

## 21.1. Методология STRIDE

| Буква | Угроза | Что означает | Примеры в Workspace |
|-------|--------|-------------|-------------------|
| **S** | Spoofing | Подмена личности / идентификатора | Поддельное устройство, фишинговый Бэк, подмена разработчика |
| **T** | Tampering | Изменение данных / кода | Модификация пакета обновления, подмена CRDT-операции, patch приложения |
| **R** | Repudiation | Отрицание совершения действия | Owner отключил аудит → выполнил действие → включил обратно |
| **I** | Information Disclosure | Утечка данных | Эксфильтрация уровня 3, чтение чужого кэша, clipboard между Space |
| **D** | Denial of Service | Отказ в обслуживании | Flood CRDT-операциями, переполнение диска бэкапами, зависание Фронта |
| **E** | Elevation of Privilege | Повышение привилегий | Побег из V8 Isolate, обход capability, захват Owner-прав через техподдержку |

---

## 21.2. Компонент: Фронт (Shell)

| Угроза | Описание | Митигация | Статус |
|--------|----------|-----------|--------|
| **S** | Поддельный Фронт маскируется под легитимный, подключается к Бэку | Device key + challenge-response (Ed25519) | ✅ Реализовано |
| **T** | Модификация кэша Фронта на диске → подмена данных в offline-режиме | Кэш зашифрован device key; при краже — недоступен | ✅ Реализовано |
| **R** | Пользователь отрицает, что открывал файл / отправлял сообщение | Аудит на Бэке; Фронт не контролирует аудит-лог | ✅ Реализовано |
| **I** | Кража устройства → чтение кэша Фронта | Шифрование кэша + screen lock + remote wipe ([L-Sec 18.1]) | ✅ Специфицировано |
| **D** | Фронт падает из-за багнутого приложения → все Space недоступны | Isolate per app; падение одного не ломает остальные | ✅ Реализовано |
| **E** | Приложение выходит за пределы V8 Isolate → читает память других приложений | V8 Sandbox + seccomp + separate process ([L-Sec 12.2]) | ✅ Специфицировано |

---

## 21.3. Компонент: Бэк (Backoffice)

| Угроза | Описание | Митигация | Статус |
|--------|----------|-----------|--------|
| **S** | Поддельный Бэк принимает подключения от Фронтов | Device key pinning + ECDH handshake + WireGuard | ✅ Реализовано |
| **T** | Модификация SQLite Бэка на диске | Шифрование at rest (AES-256-GCM) + integrity checks | ✅ Реализовано |
| **R** | Owner отключает аудит → выполняет действия → включает обратно | Immutable audit store (append-only WAL) ([L-Sec 18.6]) | ✅ Специфицировано |
| **I** | Взлом Бэка → доступ ко всем данным всех пользователей | Шифрование SQLite + ключи на TPM; минимизация данных на сервере | ✅ Реализовано |
| **D** | DoS на Бэк: flood API-запросами от приложения | Rate limiting API ([L-Sec 20.3]) | ⚠️ Специфицировано |
| **E** | Приложение обходит capability-проверку Бэка | Бэк проверяет каждый запрос; capability context невозможно подделать без приватного ключа | ✅ Реализовано |

---

## 21.4. Компонент: P2P Mesh + CRDT

| Угроза | Описание | Митигация | Статус |
|--------|----------|-----------|--------|
| **S** | Поддельное устройство в P2P-сети | Device key + WireGuard туннель; без ключа — узел не принимается | ✅ Реализовано |
| **T** | Подмена CRDT-операции в полёте | WireGuard шифрует весь трафик; целостность на уровне CRDT | ✅ Реализовано |
| **R** | Узел отрицает, что отправлял вредоносную операцию | Аудит CRDT-операций: каждая подписана device key | ✅ Реализовано |
| **I** | Перехват P2P-трафика → чтение данных | WireGuard + end-to-end encryption; промежуточные узлы не видят содержимое | ✅ Реализовано |
| **D** | Компрометированный узел генерирует миллионы CRDT-операций | Rate limiting + CRDT quarantine ([L-Sec 14]) | ✅ Специфицировано |
| **E** | Узел пытается принудительно применить чужие операции | CRDT convergence + проверка подписи device key на каждой операции | ✅ Реализовано |

---

## 21.5. Компонент: App Runtime (V8 Isolates)

| Угроза | Описание | Митигация | Статус |
|--------|----------|-----------|--------|
| **S** | Приложение маскируется под системное | App Registry + подпись пакетов; системные приложения встроены | ✅ Реализовано |
| **T** | Приложение модифицирует свой код во время выполнения | V8 Isolate не позволяет self-modifying code без перезагрузки | ✅ Реализовано |
| **R** | Приложение отрицает, что читало файл | Аудит API-вызовов; приложение не может удалить лог | ✅ Реализовано |
| **I** | Уровень 3: приложение выкачивает SQLite и отправляет наружу | Whitelist доменов + network audit + behavioural analysis ([L-Sec 10]) | ✅ Специфицировано |
| **D** | Приложение в бесконечном цикле → 100% CPU | Scheduler квот; Isolate привязан к квоте CPU | ✅ Реализовано |
| **E** | Побег из V8 Isolate → доступ к хост-ОС | seccomp-bpf + namespaces + V8 Sandbox ([L-Sec 12.2]) | ✅ Специфицировано |

---

## 21.6. Компонент: App Registry + Обновления

| Угроза | Описание | Митигация | Статус |
|--------|----------|-----------|--------|
| **S** | Поддельный магазин приложений | Certificate pinning для pkg.core.app + подпись Ed25519 | ✅ Специфицировано |
| **T** | Подмена пакета обновления в transit / at rest | Подпись Ed25519 + хеш BLAKE3; Бэк проверяет перед установкой | ✅ Реализовано |
| **R** | Разработчик отрицает, что выпустил вредоносную версию | Подпись пакета неотрицаема; все версии в Registry + аудит | ✅ Реализовано |
| **I** | Скомпрометированный ключ разработчика → подпись вредоносного пакета | Registry of developer keys + отзыв; certificate pinning ([L-Sec 18.4]) | ✅ Специфицировано |
| **D** | Удаление/блокировка App Registry → приложения не устанавливаются | Локальный кэш пакетов; P2P-репликация Registry между устройствами | ✅ Реализовано |
| **E** | Приложение обновляется без ведома пользователя до более привилегированной версии | Owner подтверждает обновление Бэка; Фронт обновляется сам, но без расширения прав | ✅ Реализовано |

---

## 21.7. Компонент: Key Manager + Auth

| Угроза | Описание | Митигация | Статус |
|--------|----------|-----------|--------|
| **S** | Подмена устройства при подключении к Бэку | Challenge-response на Ed25519; device key привязан к железу (TPM) | ✅ Реализовано |
| **T** | Модификация приватного ключа на диске | TPM/Secure Enclave; если нет TPM — зашифрован ключом biometry/PIN | ✅ Реализовано |
| **R** | Пользователь отрицает, что подключал устройство | Аудит подключений с подписью device key | ✅ Реализовано |
| **I** | Извлечение master key из RAM (cold boot) | Master key в TPM/Secure Enclave; если в RAM — обнуляется при lock | ✅ Специфицировано |
| **D** | Brute-force device key | Ed25519 ключи, 256 бит; rate limiting на handshake | ✅ Реализовано |
| **E** | Приложение получает доступ к ключам другого приложения | Derived keys per app; приложение видит только свой контекст | ✅ Реализовано |

---

## 21.8. Компонент: Clipboard + Мульти-Бэк

| Угроза | Описание | Митигация | Статус |
|--------|----------|-----------|--------|
| **S** | Подмена содержимого clipboard при копировании | Clipboard контролируется Фронтом; стороннее ПО хост-ОС не имеет доступа | ✅ Реализовано |
| **T** | Изменение политики clipboard без ведома Owner | Политика передаётся с Бэка и подписана; Фронт проверяет подпись | ✅ Реализовано |
| **R** | Пользователь отрицает копирование конфиденциальных данных | Аудит clipboard-операций (опционально, по политике Owner) | ✅ Реализовано |
| **I** | Скопированный пароль утекает в личный Space | `clipboard_allow_export: none` для корпоративных Space + подтверждение при вставке ([L-Sec 13]) | ✅ Специфицировано |
| **D** | Clipboard flood: приложение копирует гигабайты в буфер | Лимит размера элемента + лимит стека; отсечение по размеру | ✅ Специфицировано |
| **E** | Приложение читает clipboard другого Space | Изоляция clipboard per Space; API доступен только внутри своего Space | ✅ Реализовано |

---

## 21.9. Компонент: Storage (VFS + носители)

| Угроза | Описание | Митигация | Статус |
|--------|----------|-----------|--------|
| **S** | Поддельный USB-накопитель с вредоносными файлами | Host Shim проверяет тип ФС и метаданные; автозапуск отключён | ✅ Реализовано |
| **T** | Модификация файлов на внешнем носителе после импорта | Импорт = копирование + верификация хеша; оригинал не используется | ✅ Реализовано |
| **R** | Пользователь отрицает, что импортировал файл | Аудит импорта/экспорта (категория «files») | ✅ Реализовано |
| **I** | Данные на USB не зашифрованы → утекают при потере | Экспорт шифруется по опции; по умолчанию — предупреждение | ✅ Специфицировано |
| **D** | Импорт бесконечного цикла (symlink loop) | Host Shim ограничивает глубину рекурсии; таймаут операции | ✅ Специфицировано |
| **E** | Приложение получает доступ к файлам хост-ОС в обход VFS | Все пути маршрутизируются через Бэк; приложение не знает путей хост-ОС | ✅ Реализовано |

---

## 21.10. Компонент: Voice + Intent API

| Угроза | Описание | Митигация | Статус |
|--------|----------|-----------|--------|
| **S** | Поддельная voice-команда (deepfake аудио) | Whisper local-only; нет отправки аудио на сервер; проверка speaker identity (опционально) | ✅ Реализовано |
| **T** | Подмена Whisper model → вредоносная интерпретация команд | Integrity check модели (BLAKE3) ([L-Sec 18.5]) | ✅ Специфицировано |
| **R** | Пользователь отрицает голосовую команду | Локальное логирование voice-команд (опционально, по политике Owner) | ⚠️ Специфицировано |
| **I** | Аудио сохраняется после распознавания | Аудио-буфер обнуляется после распознавания; нет хранения | ✅ Специфицировано |
| **D** | Flood voice-команд → 100% CPU Whisper | Rate limiting на voice input; очередь команд с приоритетом | ✅ Специфицировано |
| **E** | Voice-команда выполняет привилегированную операцию без подтверждения | Sensitive операции требуют подтверждения (PIN / биометрия) через Intent API | ✅ Специфицировано |

---

## 21.11. Компонент: Техподдержка (Remote Support)

| Угроза | Описание | Митигация | Статус |
|--------|----------|-----------|--------|
| **S** | Поддельный специалист техподдержки | Подпись Ed25519 (HSM CORE Corp); без валидной подписи — подключение невозможно | ✅ Реализовано |
| **T** | Модификация логов техподдержки | Аудит-лог append-only; специалист не имеет доступа к логам | ✅ Реализовано |
| **R** | Специалист отрицает, что удалял данные | Все действия логируются; сессия записывается (для уровня Full) | ✅ Специфицировано |
| **I** | Специалист видит данные пользователя | Только Бэк, не Фронт; нет доступа к экрану пользователя | ✅ Реализовано |
| **D** | Спам запросами техподдержки | Бэк глух без кода; код одноразовый с TTL 10 мин | ✅ Реализовано |
| **E** | Специалист повышает права до Owner | Dual approval + видеозапись + HSM key ceremony ([L-Sec 18.3]) | ✅ Специфицировано |

---

## 21.12. Компонент: Update Engine

| Угроза | Описание | Митигация | Статус |
|--------|----------|-----------|--------|
| **S** | Поддельный update-сервер | Certificate pinning + подпись Ed25519 + хеш BLAKE3 | ✅ Реализовано |
| **T** | Модификация пакета в transit / at rest | Подпись + хеш; Бэк проверяет перед установкой | ✅ Реализовано |
| **R** | Разработчик отрицает выпуск вредоносной версии | Подпись неотрицаема; все версии в Registry + аудит | ✅ Реализовано |
| **I** | Утечка changelog / метаданных обновления | HTTPS + WireGuard; метаданные не содержат sensitive data | ✅ Реализовано |
| **D** | Flood проверками обновлений | Rate limiting на poll (раз в час по умолчанию) | ✅ Специфицировано |
| **E** | Приложение обновляется до более привилегированной версии | Owner подтверждает обновление Бэка; Фронт обновляется без расширения прав | ✅ Реализовано |

---

## 21.13. Компонент: Backup Engine

| Угроза | Описание | Митигация | Статус |
|--------|----------|-----------|--------|
| **S** | Поддельный backup-файл | Backup подписан device key; восстановление проверяет подпись | ✅ Специфицировано |
| **T** | Модификация backup на диске | Шифрование AES-256-GCM + integrity check (BLAKE3) | ✅ Реализовано |
| **R** | Owner отрицает создание backup | Аудит (категория «system») | ✅ Реализовано |
| **I** | Backup не зашифрован → утечка при краже диска | Шифрование at rest; ключ на TPM/Secure Enclave | ✅ Реализовано |
| **D** | Переполнение диска бэкапами | Retention policy; автоудаление старых снапшотов | ✅ Специфицировано |
| **E** | Восстановление backup даёт доступ к старым правам | RBAC пересчитывается при восстановлении; revoked capabilities не восстанавливаются | ⚠️ Специфицировано |

---

## 21.14. Компонент: Error Reporting Engine

| Угроза | Описание | Митигация | Статус |
|--------|----------|-----------|--------|
| **S** | Поддельный error_reporting_url | Mandatory HTTPS; HTTP отклоняется | ✅ Реализовано |
| **T** | Модификация отчёта в transit | TLS (внутри WireGuard) | ✅ Реализовано |
| **R** | Разработчик отрицает получение отчёта | Аудит отправки (категория «apps») | ✅ Реализовано |
| **I** | Эксфильтрация через Error.message | Rate limit 3/час; preview перед отправкой; whitelist полей | ✅ Специфицировано |
| **D** | Flood ошибками → спам разработчику | Rate limiting + блокировка + предупреждение пользователю | ✅ Специфицировано |
| **E** | Приложение получает доступ к error reporting API | Нет API для произвольной отправки; только auto-capture | ✅ Реализовано |

---

## 21.15. Компонент: Energy Manager

| Угроза | Описание | Митигация | Статус |
|--------|----------|-----------|--------|
| **S** | Поддельный thermal profile | Подпись профиля device key; только Owner может менять | ⚠️ Специфицировано |
| **T** | Модификация governor settings | Изменения логируются в аудит (категория «system») | ⚠️ Специфицировано |
| **R** | Owner отрицает отключение CPU throttling | Аудит (категория «system») | ✅ Реализовано |
| **I** | Утечка power consumption patterns | Данные не покидают устройство; не синхронизируются | ✅ Реализовано |
| **D** | DoS через thermal throttling (блокировка CPU) | Fallback на базовый governor; аппаратная защита от перегрева | ⚠️ Специфицировано |
| **E** | Приложение повышает приоритет до системного | Scheduler квоты; приложение не может менять свою квоту | ✅ Реализовано |

---

## 21.16. Компонент: Display Server

| Угроза | Описание | Митигация | Статус |
|--------|----------|-----------|--------|
| **S** | Поддельное окно маскируется под системное | Window Manager проверяет app_id + digital signature; системные окна имеют special flag | ⚠️ Специфицировано |
| **T** | Модификация Theme Engine → вредоносные шейдеры | Шейдеры загружаются только из signed пакетов; sandbox компиляции | ⚠️ Специфицировано |
| **R** | Пользователь отрицает действие в UI | Аудит UI-взаимодействий (категория «apps») | ⚠️ Специфицировано |
| **I** | Утечка через GPU buffers | GPU buffer cleanup при freeze/thaw профиля; isolated memory per Space | ✅ Специфицировано |
| **D** | Багнутый шейдер → зависание GPU | Shader validation; watchdog таймаут; fallback на Software Renderer | ⚠️ Специфицировано |
| **E** | Приложение читает framebuffer другого окна | Изоляция GPU contexts per Space; compositor не даёт доступ к чужому framebuffer | ⚠️ Специфицировано |

---

## 21.17. Компонент: Generative UI + Smart Scheduler

| Угроза | Описание | Митигация | Статус |
|--------|----------|-----------|--------|
| **S** | Prompt injection → генерация фишингового UI | Input sanitization; Intent Whitelist; sandbox-валидация перед рендерингом | ⚠️ Специфицировано |
| **T** | Модификация generated UI в transit | TLS + WireGuard; generated UI не кэшируется persistently | ⚠️ Специфицировано |
| **R** | Пользователь отрицает голосовую команду | Локальное логирование voice-команд (опционально) | ⚠️ Специфицировано |
| **I** | Prompt содержит sensitive data | Prompt filtering удаляет чувствительные данные перед отправкой в LLM | ✅ Специфицировано |
| **D** | Flood генерацией UI → 100% GPU | Rate limiting на Generative UI; Scheduler квоты | ⚠️ Специфицировано |
| **E** | Generated UI выполняет привилегированную операцию | Intent Whitelist + capabilities check; sensitive операции требуют PIN/биометрии | ✅ Специфицировано |

---

## 21.18. Компонент: Notifications

| Угроза | Описание | Митигация | Статус |
|--------|----------|-----------|--------|
| **S** | Поддельное уведомление от имени системы | System notifications подписаны Бэком; приложения не могут подделать системный flag | ⚠️ Специфицировано |
| **T** | Модификация текста уведомления в transit | TLS + WireGuard; подпись device key | ✅ Реализовано |
| **R** | Приложение отрицает отправку спам-уведомления | Аудит (категория «apps») | ✅ Реализовано |
| **I** | Уведомление содержит sensitive data | Notification preview настраивается пользователем; lock screen скрывает content | ⚠️ Специфицировано |
| **D** | Спам-уведомления → DoS | Rate limiting per app; priority queue; пользователь может заблокировать приложение | ✅ Специфицировано |
| **E** | Приложение отправляет уведомления без permissions | Permissions UI контролирует `notifications` capability | ✅ Реализовано |

---

## 21.19. Компонент: Command Bar + Input Router

| Угроза | Описание | Митигация | Статус |
|--------|----------|-----------|--------|
| **S** | Поддельная строка (фишинговый UI поверх) | Command Bar рендерится Display Server'ом; overlay detection | ⚠️ Специфицировано |
| **T** | Модификация истории команд | История хранится в SQLite Бэка; подписана device key | ✅ Реализовано |
| **R** | Пользователь отрицает введённую команду | Аудит поисковых запросов (категория «search») | ✅ Реализовано |
| **I** | Утечка истории через suggestions | Suggestions формируются локально; не отправляются на сервер | ✅ Реализовано |
| **D** | Flood команд → 100% CPU Intent Parser | Rate limiting; очередь с приоритетом; user-visible > background | ✅ Специфицировано |
| **E** | Command injection через строку | Input sanitization; parameterized Intents; нет прямого exec | ✅ Реализовано |

---

## 21.20. Компонент: VFS (Mirror Engine + Lazy Load + Deduplication)

| Угроза | Описание | Митигация | Статус |
|--------|----------|-----------|--------|
| **S** | Поддельный mirror-target | Device key pinning; target доказывает владение ключом | ✅ Реализовано |
| **T** | Модификация mirrored файла на целевом устройстве | CRDT convergence + подпись каждой операции; Merkle root validation | ✅ Реализовано |
| **R** | Узел отрицает удаление файла | Аудит CRDT-операций (категория «files») | ✅ Реализовано |
| **I** | Timing attack через deduplication | Deduplication происходит на уровне блоков; padding для одинакового размера | ⚠️ Специфицировано |
| **D** | DoS через lazy load loop (бесконечная подгрузка) | Таймаут lazy load; max retries; fallback на offline-режим | ⚠️ Специфицировано |
| **E** | Приложение получает доступ к чужому mirrored файлу | RBAC + capability context; mirror scope ограничен проектом | ✅ Реализовано |

---

## 21.21. Компонент: P2P TCP Relay + FEC + Bit-Diff

| Угроза | Описание | Митигация | Статус |
|--------|----------|-----------|--------|
| **S** | Поддельный relay-сервер | Certificate pinning для relay; подпись Ed25519 | ⚠️ Специфицировано |
| **T** | Модификация relayed пакета | WireGuard шифрует весь трафик; relay видит только метаданные (размер, тайминги) | ✅ Реализовано |
| **R** | Relay-оператор отрицает перехват | Аудит подключений к relay (категория «multiback») | ⚠️ Специфицировано |
| **I** | Information leakage через Bit-Diff size | Padding дельт до фиксированного размера; batching операций | ⚠️ Специфицировано |
| **D** | DoS через relay flood | Rate limiting per device; relay drop при превышении | ⚠️ Специфицировано |
| **E** | Relay пытается внедрить свои операции | Relay не имеет device key; все CRDT-операции подписаны | ✅ Реализовано |

---

## 21.22. Сводная таблица по компонентам

| Компонент | S | T | R | I | D | E | Приоритет |
|-----------|---|---|---|---|---|---|-----------|
| Фронт (Shell) | ✅ | ✅ | ✅ | ⚠️ | ✅ | ⚠️ | Средний |
| Бэк (Backoffice) | ✅ | ✅ | ⚠️ | ✅ | ⚠️ | ✅ | Высокий |
| P2P Mesh + CRDT | ✅ | ✅ | ✅ | ✅ | ⚠️ | ✅ | Средний |
| App Runtime | ✅ | ✅ | ✅ | ⚠️ | ✅ | ⚠️ | **Критичный** |
| App Registry | ⚠️ | ✅ | ✅ | ⚠️ | ✅ | ✅ | Высокий |
| Key Manager + Auth | ✅ | ✅ | ✅ | ⚠️ | ✅ | ✅ | **Критичный** |
| Clipboard + Мульти-Бэк | ✅ | ✅ | ✅ | ⚠️ | ⚠️ | ✅ | Средний |
| Storage (VFS) | ✅ | ✅ | ✅ | ⚠️ | ⚠️ | ✅ | Средний |
| Voice + Intent API | ⚠️ | ⚠️ | ⚠️ | ⚠️ | ⚠️ | ⚠️ | Средний |
| Техподдержка | ✅ | ✅ | ⚠️ | ✅ | ✅ | ⚠️ | Высокий |
| Update Engine | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | Высокий |
| Backup Engine | ✅ | ✅ | ✅ | ✅ | ⚠️ | ⚠️ | Средний |
| Error Reporting Engine | ✅ | ✅ | ✅ | ✅ | ⚠️ | ✅ | Средний |
| Energy Manager | ⚠️ | ⚠️ | ✅ | ✅ | ⚠️ | ✅ | Низкий |
| Display Server | ⚠️ | ⚠️ | ⚠️ | ✅ | ⚠️ | ⚠️ | Средний |
| Generative UI | ⚠️ | ⚠️ | ⚠️ | ✅ | ⚠️ | ✅ | Высокий |
| Notifications | ⚠️ | ✅ | ✅ | ⚠️ | ✅ | ✅ | Средний |
| Command Bar | ⚠️ | ✅ | ✅ | ✅ | ✅ | ✅ | Средний |
| VFS (Mirror/Lazy/Dedup) | ✅ | ✅ | ✅ | ⚠️ | ⚠️ | ✅ | Средний |
| P2P TCP Relay + Bit-Diff | ⚠️ | ✅ | ⚠️ | ⚠️ | ⚠️ | ✅ | Средний |
| Idle CPU Policy | ⚠️ | ✅ | ✅ | ⚠️ | ✅ | ⚠️ | Средний |
| Golden Image (SquashFS) | ✅ | ✅ | ✅ | ⚠️ | ⚠️ | ✅ | Высокий |
| CPU Affinity + SCHED_RR | ⚠️ | ✅ | ⚠️ | ⚠️ | ✅ | ⚠️ | Средний |
| P2P Traffic Priority | ⚠️ | ✅ | ⚠️ | ⚠️ | ✅ | ✅ | Средний |
| Game Mode API | ⚠️ | ✅ | ✅ | ⚠️ | ✅ | ⚠️ | Высокий |
| Secure Transaction API | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | **Критичный** |
| Public P2P CDN (Verified Seeding) | ⚠️ | ✅ | ✅ | ⚠️ | ✅ | ✅ | Высокий |

**Легенда:** ✅ — митигация реализована или специфицирована; ⚠️ — частично или требует доработки при имплементации.

---

## 21.23. Accepted Risks

| Риск | Почему принят | Компенсирующие контроли |
|------|---------------|------------------------|
| Доверие к хост-ОС | Архитектурное ограничение: Workspace — слой поверх хоста | Integrity Monitoring, remote attestation, dedicated device (Core Base) |
| Владелец = root | Самоуправляемая система не может ограничивать владельца | Immutable audit, опциональный external audit для корпоративных Бэков |
| Recovery-фраза = единая точка отказа | BIP-39 — стандарт; Shamir требует сложности для пользователя | Social recovery, HSM-based recovery для корпоративных |
| Всегда активный микрофон | Voice-first UX требует постоянного слушания | LED-индикатор, аудио-буфер обнуляется, opt-out |

---

## 21.24. Рекомендации по дальнейшей работе

1. **Формализовать seccomp-профили** — конкретные списки разрешённых syscall для каждого уровня приложений
2. **Реализовать behavioural analysis** — ML-модель для выявления аномалий в API-вызовах и сетевом трафике
3. **Провести пентест V8 Isolate** — независимый аудит sandbox при первом релизе
4. **Сертифицировать HSM-процедуры** — FIPS 140-2 Level 3 для ключей CORE Corp
5. **Автоматизировать threat model review** — при изменении архитектуры пересматривать затронутые компоненты


## 22. Security Architecture

Конкретные реализации безопасности: seccomp-профили, namespace-конфиги, memory protection, boot integrity. Для команды реализации Host Shim и Micro-Kernel.

**Статус:** спецификация для MVP (реализация — Host Shim на Rust + Micro-Kernel на Bun).  
**Ссылки:** этот документ, [layer-6-apps.md](layer-6-apps.md).

---

## 22.1. Sandbox по уровням приложений

### 22.1.1 Общая архитектура

```
Уровень 1–2: Island Mode (Chromium sandbox)
  └─ Linux: namespace + seccomp + chroot
  └─ Windows: AppContainer + Job Object
  └─ macOS: seatbelt profile

Уровень 3: Island Mode + V8 Isolate backend
  └─ Linux: namespace + seccomp-bpf + network namespace (whitelist)
  └─ Windows: AppContainer + restricted network
  └─ macOS: seatbelt + network filter

Уровень 4–5: V8 Isolate (Bun runtime)
  └─ Linux: namespace + seccomp-bpf + V8 Sandbox
  └─ Windows: AppContainer + Job Object + V8 Sandbox
  └─ macOS: seatbelt + V8 Sandbox
```

### 22.1.2 Seccomp-профили (Linux)

**Base profile** — для всех V8 Isolates:

```json
{
  "defaultAction": "SCMP_ACT_ERRNO",
  "architectures": ["SCMP_ARCH_X86_64", "SCMP_ARCH_AARCH64"],
  "syscalls": [
    {"names": ["read", "write", "close", "exit", "exit_group"], "action": "SCMP_ACT_ALLOW"},
    {"names": ["mmap", "munmap", "mprotect", "brk"], "action": "SCMP_ACT_ALLOW", "args": [
      {"index": 2, "op": "SCMP_CMP_MASKED_EQ", "value": 0x7, "mask": 0x7}
    ]},
    {"names": ["futex", "sched_yield", "clock_gettime", "gettimeofday"], "action": "SCMP_ACT_ALLOW"},
    {"names": ["rt_sigaction", "rt_sigprocmask", "sigreturn"], "action": "SCMP_ACT_ALLOW"},
    {"names": ["prctl"], "action": "SCMP_ACT_ALLOW", "args": [
      {"index": 0, "op": "SCMP_CMP_EQ", "value": 15}
    ]},
    {"names": ["openat"], "action": "SCMP_ACT_ALLOW", "args": [
      {"index": 2, "op": "SCMP_CMP_MASKED_EQ", "value": 0x3, "mask": 0x3}
    ]},
    {"names": ["socket", "connect", "sendto", "recvfrom", "getsockopt"], "action": "SCMP_ACT_ERRNO"},
    {"names": ["execve", "execveat", "fork", "vfork", "clone"], "action": "SCMP_ACT_KILL"},
    {"names": ["ptrace"], "action": "SCMP_ACT_KILL"},
    {"names": ["mount", "umount2", "pivot_root"], "action": "SCMP_ACT_KILL"},
    {"names": ["chmod", "chown", "setuid", "setgid", "setresuid"], "action": "SCMP_ACT_KILL"}
  ]
}
```

**Level 3 network profile** — дополнительно к base:

```json
{
  "extends": "base",
  "syscalls": [
    {"names": ["socket"], "action": "SCMP_ACT_ALLOW", "args": [
      {"index": 0, "op": "SCMP_CMP_EQ", "value": 2},
      {"index": 1, "op": "SCMP_CMP_EQ", "value": 1}
    ]},
    {"names": ["connect", "sendto", "recvfrom", "getsockopt", "setsockopt"], "action": "SCMP_ACT_ALLOW"},
    {"names": ["bind", "listen", "accept"], "action": "SCMP_ACT_ERRNO"}
  ]
}
```

**Level 4–5 fs profile** — если приложение запросило `fs` capability:

```json
{
  "extends": "base",
  "syscalls": [
    {"names": ["openat", "read", "write", "close", "lseek", "fstat", "getdents64"], "action": "SCMP_ACT_ALLOW"},
    {"names": ["mkdir", "unlink", "rename"], "action": "SCMP_ACT_ALLOW", "args": [
      {"index": 0, "op": "SCMP_CMP_MASKED_EQ", "value": "/vfs/app/**", "mask": null}
    ]}
  ]
}
```

> **Примечание:** seccomp-bpf не фильтрует по пути — путь фильтруется на уровне Micro-Kernel (Capability Security). seccomp блокирует опасные syscall категориями.

### 22.1.3 Windows AppContainer

**SID capabilities для уровней 3–5:**

```cpp
// Base capability
S-1-15-3-1  // internetClient (только для уровня 3, если whitelist пуст)

// Level 3 с whitelist → убираем internetClient, добавляем custom capability
S-1-15-3-1024-xxx  // custom: whitelist доменов через proxy (Micro-Kernel)

// Level 4–5 без network capability → полностью изолирован
// FS access через broker (Micro-Kernel)
```

**Job Object ограничения:**
- `JOB_OBJECT_LIMIT_ACTIVE_PROCESS`: 1 (только сам Isolate)
- `JOB_OBJECT_LIMIT_AFFINITY`: 2 ядра максимум
- `JOB_OBJECT_LIMIT_WORKINGSET`: 512MB default, настраивается квотой
- `JOB_OBJECT_LIMIT_KILL_ON_JOB_CLOSE`: автоматическое уничтожение при закрытии

### 22.1.4 macOS Seatbelt

**Base profile (V8 Isolate):**

```scheme
(version 1)
(deny default)
(allow signal)
(allow ipc-posix-shm)
(allow sysctl-read)
(allow mach-lookup)

; Чтение исполняемого кода и shared libraries
(allow file-read*
  (subpath "/System/Library")
  (subpath "/usr/lib")
  (subpath "{{APP_BUNDLE}}/Contents/Frameworks"))

; Запрет сети (для уровней 4-5)
(deny network*)

; Для уровня 3 — разрешение через Micro-Kernel proxy
(allow network-outbound
  (remote ip "{{WHITELIST_IPS}}"))
```

---

## 22.2. Namespace конфигурации (Linux)

### 22.2.1 Полная изоляция уровня 3–5

```bash
unshare --pid --net --mount --ipc --uts --user --fork \
  --mount-proc=/proc \
  --pid-file=/run/core/isolate_{{APP_ID}}.pid \
  /opt/core/runtime/isolate_launcher {{APP_ID}}
```

**Что изолируется:**

| Namespace | Что изолировано | Зачем |
|-----------|----------------|-------|
| PID | Собственное дерево процессов | Isolate не видит другие приложения |
| Network | Отдельный loopback + veth к Micro-Kernel | Контроль трафика на уровне ядра |
| Mount | Только /vfs/app/{{APP_ID}} + ro /system | Нет доступа к хост-ФС |
| IPC | Собственные SysV IPC + POSIX shm | Нет перехвата сообщений других приложений |
| UTS | Собственное hostname | Минимизация информации о хосте |
| User | UID 0 внутри = непривилегированный снаружи | Побег из namespace ≠ root |

### 22.2.2 Mount layout внутри Isolate

```
/
├── proc          # ro, mount-proc (только свои процессы)
├── sys           # ro, пустой или минимальный
├── dev           # минимальный (null, zero, random, urandom, tty)
├── vfs/
│   └── app/
│       └── {{APP_ID}}/  # rw, app-scoped VFS
├── system/
│   └── lib/       # ro, shared libraries runtime
└── tmp/           # tmpfs, ограниченный размер (100MB)
```

---

## 22.3. V8 Sandbox (memory corruption mitigation)

### 22.3.1 Параметры запуска

```bash
--v8-sandbox
--v8-sandbox-pointer-compression
--v8-sandbox-page-size=262144  # 256KB sandbox pages
--v8-sandbox-gc
```

### 22.3.2 Резервирование address space

```rust
// Host Shim (Rust)
let sandbox_size = 1 << 40; // 1TB address space reservation
let sandbox_base = unsafe {
    libc::mmap(
        null_mut(),
        sandbox_size,
        PROT_NONE,
        MAP_PRIVATE | MAP_ANONYMOUS | MAP_NORESERVE,
        -1,
        0,
    )
};
// Передаём sandbox_base в V8 Isolate при создании
```

### 22.3.3 Runtime integrity check

```rust
// Периодическая проверка (каждые 30 сек)
fn verify_isolate_memory(isolate: &Isolate) -> Result<(), SecurityError> {
    let heap = isolate.get_heap_statistics();
    // Проверка: все указатели внутри sandbox
    // Проверка: нет unexpected executable pages
    // Проверка: stack canaries intact
    isolate.verify_pointer_cage()
}
```

---

## 22.4. Memory Protection

### 22.4.1 Анонимный профиль (mlock)

```rust
#[cfg(target_os = "linux")]
fn secure_anonymous_profile(data: &mut [u8]) {
    unsafe {
        libc::mlock(data.as_ptr() as *const c_void, data.len());
    }
}

#[cfg(target_os = "windows")]
fn secure_anonymous_profile(data: &mut [u8]) {
    unsafe {
        winapi::um::memoryapi::VirtualLock(
            data.as_ptr() as LPVOID,
            data.len() as SIZE_T,
        );
    }
}
```

### 22.4.2 Secure wipe при freeze профиля

```rust
fn secure_wipe_profile(profile_id: &str) {
    // 1. Очистка V8 heap
    for isolate in get_isolates_by_profile(profile_id) {
        isolate.terminate_execution();
        isolate.dispose();
    }
    
    // 2. Очистка SharedArrayBuffer regions
    for region in get_sab_regions(profile_id) {
        region.zeroize(); // zeroize crate
        drop(region);
    }
    
    // 3. Очистка clipboard
    clipboard::clear_by_profile(profile_id);
    
    // 4. Очистка GPU buffers (WebGPU)
    gpu::clear_buffers_by_profile(profile_id);
}
```

### 22.4.3 Encrypted memory (уровень «Максимальный»)

```rust
#[cfg(feature = "max_security")]
mod encrypted_memory {
    use aes_gcm_siv::{Aes256GcmSiv, Nonce};
    
    pub struct EncryptedPage {
        ciphertext: Vec<u8>,
        nonce: Nonce,
        tag: [u8; 16],
    }
    
    // Данные расшифровываются только при доступе, шифруются после use
    // Реализация через page fault handler (mprotect + SIGSEGV)
}
```

---

## 22.5. Boot Integrity

### 22.5.1 Core Base / Raspberry Pi

```bash
# LUKS encrypted rootfs
cryptsetup luksFormat /dev/mmcblk0p2
cryptsetup open /dev/mmcblk0p2 core-root
mkfs.ext4 /dev/mapper/core-root

# Boot partition — только signed kernel + initrd
# initrd проверяет подпись rootfs перед mount
```

### 22.5.2 Boot-time passphrase via QR (Core Base)

```
Core Base загружается → на HDMI показывает QR-код
└─ QR содержит: ephemeral public key + device nonce

Приложение CORE на телефоне:
└─ Сканирует QR → генерирует shared secret (ECDH)
└─ Шифрует passphrase shared secret
└─ Отправляет encrypted passphrase в Core Base (BLE / LAN)

Core Base:
└─ Расшифровывает passphrase
└─ Открывает LUKS
└─ Уничтожает passphrase из RAM (explicit zeroize)
```

### 22.5.3 Integrity Monitoring Agent

```rust
// Периодическая проверка (каждые 5 мин)
fn check_integrity() -> IntegrityReport {
    let expected_hash = get_expected_binary_hash(); // от Бэка или embedded
    let actual_hash = blake3::hash(std::fs::read("/opt/core/bin/workspace").unwrap());
    
    if expected_hash != actual_hash {
        IntegrityReport::Tampered {
            expected: expected_hash,
            actual: actual_hash,
            timestamp: now(),
        }
    } else {
        IntegrityReport::Ok
    }
}
```

### 22.5.4 Immutable Rootfs (OverlayFS + dm-verity)

**Что:** Системные файлы Workspace неизменяемы на уровне файловой системы. Обновления — атомарная смена lowerdir OverlayFS.

**Как:**

```
Core Base (Linux bare metal):
  |
  +-- dm-verity на lowerdir (/usr/core/):
  |   +-- При загрузке: ядро проверяет root hash всего lowerdir
  |   +-- Любая модификация → hash mismatch → panic / safe mode
  |   +-- Root hash встроен в signed kernel image (EFI Secure Boot)
  |
  +-- OverlayFS:
  |   +-- lowerdir (verity): /usr/core/v1/ (read-only, verified)
  |   +-- upperdir: /var/core/userdata/ (encrypted, read-write)
  |   +-- workdir: /var/core/.work/
  |   +-- merged: то, что видит runtime
  |
  +-- Atomic update:
      +-- Новая версия v2 скачивается в /var/core/updates/v2/
      +-- Verity root hash v2 проверяется по подписи
      +-- Atomically remount: lowerdir = v2, upperdir сохраняется
      +-- Rollback: если v2 не стартует → remount на v1 за 3 сек
      +-- Garbage collect: v1 удаляется через 24ч после подтверждения стабильности
```

**Защита:**
- **Tampering:** модификация системного файла невозможна (verity read-only)
- **Rollback attacks:** каждый lowerdir versioned, старая версия удаляется только после подтверждения
- **Persistence:** malware не может закрепиться в system partition — только upperdir (который шифрован и периодически проверяется Integrity Monitoring Agent)
- **Golden image (SquashFS):** дополнительный read-only lowerdir, никогда не обновляется через сеть. Хранится в eMMC/flash, проверяется Integrity Monitoring Agent. Fallback на golden → mandatory notification Owner + sandbox mode (без сети) до полного обновления

**Где:** Level 0 (Host Shim, mount/verity) + Level 1 (Update Engine).

---

## 22.6. Network Isolation (уровень 3)

### 22.6.1 Micro-Kernel network proxy

```
[V8 Isolate Level 3]
  │ socket() → connect()
  │ 
  ▼
[Network Proxy (Micro-Kernel, Level 1)]
  │ Проверка: домен в whitelist манифеста?
  │ Да → проксирование через host-ОС
  │ Нет → ECONNREFUSED
  ▼
[Host-ОС network stack]
```

### 22.6.2 Firewall-правило localhost

```bash
# Только frontend Island этого приложения может стучаться на localhost:PORT
iptables -A INPUT -p tcp --dport {{APP_PORT}} \
  -m owner --uid-owner {{FRONTEND_UID}} \
  -j ACCEPT

iptables -A INPUT -p tcp --dport {{APP_PORT}} \
  -j DROP
```

> На Windows: Windows Filtering Platform (WFP)  
> На macOS: pf / application firewall

### 22.6.3 Mesh Traffic Priority (URGENT / IDLE)

**Проблема:** Злоумышленник может генерировать фейковые URGENT-пакеты, забивая канал и блокируя реальные CRDT-дельты.

**Решение:**
- URGENT-флаг в P2P-пакете подписан device key (Ed25519). Mesh Engine отбрасывает неподписанные URGENT-пакеты
- Padding: все URGENT-пакеты дополняются до фиксированного размера (traffic analysis mitigation)
- Rate limiting: max URGENT packets/sec per peer. Превышение → peer помечается suspicious, канал throttled

**Где:** Level 2 (Mesh Engine, packet filter).

### 22.6.4 Public Seeding Security

**Проблема:** Public P2P CDN — раздача контента неизвестным пользователям. Риски: пиратство, IP leak, bandwidth drain, malware distribution.

**Решения:**

```rust
fn verify_public_seed(cid: &CID, metadata: &Metadata) -> Result<()> {
    // 1. Publisher signature check
    let publisher_key = app_registry.get_publisher_key(metadata.publisher_id)?;
    if !ed25519::verify(metadata, &publisher_key) {
        return Err(SecurityError::InvalidPublisherSignature);
    }
    
    // 2. CID integrity
    let expected_cid = blake3::hash(download_content(cid)?);
    if expected_cid != *cid {
        return Err(SecurityError::CIDMismatch);
    }
    
    // 3. Category whitelist
    if !ALLOWED_CATEGORIES.contains(metadata.category) {
        return Err(SecurityError::CategoryNotAllowed);
    }
    
    // 4. User consent (opt-in)
    if !user_settings.public_seeding_enabled {
        return Err(SecurityError::SeedingDisabled);
    }
    
    Ok(())
}
```

**Защита:**
- **Legal (distribution of illegal content):** Только verified publisher content. App Registry проверяет publisher identity. Category whitelist: apps, updates, verified media
- **DoS (bandwidth drain):** Bandwidth cap 10% upload. Connection limit 50 peers/file. Rate limiting per peer
- **Information Disclosure (IP leak):** IP privacy через relay / onion routing. Direct P2P только после ECDH ephemeral handshake
- **Tampering (metadata spoofing):** Metadata подписана publisher key. CID = BLAKE3(content), неподделываемый
- **Spoofing (fake publisher):** Publisher keys зарегистрированы в App Registry с verification (domain, certificate, KYC для корпоративных)

**Где:** Level 2 (Mesh Engine, DHT + verification) + Level 1 (App Registry, publisher registry).

---

## 22.7. App Lifecycle Security Hooks

### 22.7.1 Hook points

```rust
enum SecurityHook {
    BeforeIsolateCreate { app_id, level, manifest },
    AfterIsolateCreate { isolate_id, pid },
    BeforeApiCall { isolate_id, api, args },
    AfterApiCall { isolate_id, api, result },
    BeforeNetworkRequest { isolate_id, domain, port },
    OnIsolateFreeze { isolate_id },
    OnIsolateThaw { isolate_id },
    OnIsolateSuspend { isolate_id, reason: SuspendReason },
    OnIsolateResume { isolate_id, source: ResumeSource },
    OnIsolateDestroy { isolate_id },
    OnProfileSwitch { from_profile, to_profile },
}
```

### 22.7.2 Реализация hooks (Rust)

```rust
trait SecurityHookHandler {
    fn handle(&self, hook: SecurityHook) -> HookResult;
}

struct DefaultSecurityHandler;

impl SecurityHookHandler for DefaultSecurityHandler {
    fn handle(&self, hook: SecurityHook) -> HookResult {
        match hook {
            SecurityHook::BeforeNetworkRequest { isolate_id, domain, .. } => {
                if !is_domain_whitelisted(isolate_id, domain) {
                    return HookResult::Block(NetworkError::DomainNotAllowed);
                }
                HookResult::Allow
            }
            SecurityHook::OnProfileSwitch { from_profile, .. } => {
                secure_wipe_profile(&from_profile);
                HookResult::Allow
            }
            _ => HookResult::Allow,
        }
    }
}
```

### 22.7.3 Suspend / Resume Security

**OnIsolateSuspend:**
- State сериализуется в encrypted SQLite (per-profile key). BLAKE3-хеш snapshot записывается в metadata
- RAM zeroize после сериализации (explicit memset_s)
- Suspend по причине «Холодный камень» → isolate помечается в audit log (категория «resource»)

**OnIsolateResume:**
- Проверка integrity snapshot: BLAKE3-хеш должен совпадать
- Rate limit: max 10 wake-ups/min. Превышение → isolate помечается suspicious, Owner уведомляется
- Resume source логируется: user_action | push | timer | intent

**Где:** Level 1 (Micro-Kernel, Security Hooks).

### 22.7.4 CPU Affinity Security

**Проблема:** Core Pinning даёт thread'у dedicated CPU core. Если pinned thread зависает или скомпрометирован — CPU starvation для остальных компонентов.

**Решения:**

```rust
// Integrity gate: pin только если hash совпадает
fn set_cpu_affinity(process: &Process, cores: &[usize]) -> Result<()> {
    let expected = get_expected_binary_hash(process.name)?;
    let actual = blake3::hash(std::fs::read(process.path)?);
    if expected != actual {
        return Err(SecurityError::TamperedBinary);
    }
    // Core Base (Linux): SCHED_RR (не FIFO!) + high priority
    // Windows/macOS: HIGH_PRIORITY_CLASS / QOS_CLASS_USER_INTERACTIVE (best effort)
    os::set_priority(process, Priority::High)?;
    os::pin_to_cores(process, cores)?;
    Ok(())
}

// Watchdog: если pinned thread не отвечает 100мс → restart
fn pinned_thread_watchdog() {
    loop {
        sleep(Duration::from_millis(50));
        for thread in pinned_threads() {
            if thread.last_heartbeat.elapsed() > Duration::from_millis(100) {
                log_security_event(SecurityEvent::CpuStarvation { thread_id: thread.id });
                thread.force_unpin();
                thread.restart();
            }
        }
    }
}
```

**Защита:**
- **Tampering:** pin устанавливается только после integrity check (BLAKE3 hash)
- **DoS (starvation):** watchdog с heartbeat monitor. Deadlock pinned thread → automatic unpause + restart
- **Privilege Escalation:** Core Base использует `CAP_SYS_NICE` вместо root + seccomp-бокс. Нет прав на `CAP_SYS_ADMIN`

**Где:** Level 0 (Host Shim, thread scheduler + watchdog).

### 22.7.5 Game Mode Security

**Проблема:** Game Mode даёт приложению direct GPU context + raw input + CPU affinity. Если приложение скомпрометировано — оно может читать GPU memory других приложений, перехватывать ввод, зависнуть с SCHED_RR.

**Решения:**

```rust
fn enter_game_mode(app: &App) -> Result<()> {
    // Только verified publisher (level >= 4)
    if app.level < 4 {
        return Err(SecurityError::InsufficientTrustLevel);
    }
    
    // Integrity check перед Game Mode
    let expected = app_registry.get_expected_hash(app.id)?;
    let actual = blake3::hash(app.code_blob);
    if expected != actual {
        return Err(SecurityError::TamperedBinary);
    }
    
    // GPU memory zeroize
    gpu::zeroize_framebuffer();
    gpu::isolate_memory_region(app.isolate_id);
    
    // Raw input sandbox
    input::enable_raw_mode(app.window_id); // только внутри окна
    input::reserve_system_gestures();      // Panic Gesture всегда активен
    
    // CPU affinity + SCHED_RR
    cpu::pin_and_set_priority(app.render_thread, Priority::High)?;
    
    // Watchdog heartbeat
    app.enable_heartbeat(50ms)?;
    
    Ok(())
}
```

**Защита:**
- **Information Disclosure (GPU side-channel):** GPU memory zeroize перед передачей контекста. Memory isolation per isolate (wgpu memory barriers)
- **Tampering (input interception):** Raw input работает только внутри Game Mode window. System gestures (Panic Gesture, screenshot) не перехватываются
- **DoS (deadlock):** Watchdog heartbeat 50ms. Dead isolate → forced kill + Display Server recovery
- **Spoofing (fake game):** Game Mode только для level >= 4 (verified publisher) + explicit user confirmation + integrity check

**Где:** Level 0 (Host Shim, GPU/input scheduler) + Level 3 (Display Server, window isolation).

### 22.7.6 Secure Transaction Security

**Проблема:** Приложение просит подписать данные. Пользователь должен точно знать, что подписывает. Приложение не должно видеть приватный ключ.

**Решения:**

```rust
fn secure_sign(app: &App, data: &[u8], key_id: &str) -> Result<Signature> {
    // Capability check
    if !app.capabilities.contains("secure_sign") {
        return Err(SecurityError::CapabilityDenied);
    }
    
    // Rate limiting
    if app.sign_count_this_hour > 100 {
        return Err(SecurityError::RateLimitExceeded);
    }
    
    // WYSIWYS overlay (Display Server renders, not app)
    let tx_details = parse_transaction(data)?;
    display_server.show_confirmation_overlay(tx_details)?;
    
    // Biometric prompt (Host Shim, not app)
    let biometric_ok = host_shim.prompt_biometry()?;
    if !biometric_ok {
        return Err(SecurityError::BiometryDenied);
    }
    
    // Signing in Secure Enclave (private key never leaves)
    let signature = secure_enclave::sign(data, key_id)?;
    
    // Audit log
    audit::log(AuditEvent::TransactionSigned {
        app_id: app.id,
        key_id: key_id.to_string(),
        data_hash: blake3::hash(data),
        timestamp: now(),
    });
    
    Ok(signature)
}
```

**Защита:**
- **Tampering (MITM):** WYSIWYS overlay рендерит Display Server (система), не приложение. Пользователь видит точные детали транзакции
- **Spoofing (фишинг):** Overlay — системный, приложение не может его подделать. Biometric prompt через Host Shim (OS API)
- **Information Disclosure (side-channel):** Constant-time signing (Ed25519). Biometric result — булево, без timing leak
- **Repudiation:** Audit log с timestamp, data hash, signature, app_id. Неотъемлемое доказательство

**Где:** Level 0 (Host Shim, Secure Enclave) + Level 1 (Micro-Kernel, capability + rate limit) + Level 3 (Display Server, WYSIWYS overlay).

---

## 22.8. Session Management

### 22.8.1 TTL и auto-lock

```rust
struct SessionConfig {
    ttl_minutes: u32,           // 30 по умолчанию
    auto_lock_after_idle: u32,  // 5 минут простоя
    require_biometry: bool,     // true для уровня «Повышенный»
}

// Проверка каждую минуту
fn check_session_expiry(session: &Session) -> SessionAction {
    if session.idle_duration() > session.config.auto_lock_after_idle {
        SessionAction::Lock
    } else if session.total_duration() > session.config.ttl_minutes {
        SessionAction::Logout
    } else {
        SessionAction::Continue
    }
}
```

### 22.8.2 Принудительный logout Owner-ом

```rust
// Owner через Core.Hardcore или другой Фронт
fn force_logout(device_key: &DeviceKey) {
    // 1. Отзыв session token на Бэке
    backend.revoke_session(device_key);
    
    // 2. Уведомление Фронту (push)
    push_notification(device_key, Notification::SessionRevoked);
    
    // 3. Фронт: lock screen + очистка кэша
    frontend.lock_and_purge(device_key);
}
```

---

## 22.9. Supply Chain Protection

### 22.9.1 Pinning зависимостей

```toml
# Cargo.lock — коммитится в репозиторий
# Для каждого crate:
[package]
name = "wgpu"
version = "0.19.0"
source = "registry+https://github.com/rust-lang/crates.io-index"
checksum = "blake3::hash(...)"  # Верификация при сборке
```

```json
// bun.lockb — коммитится
// package.json — exact versions, no ^ or ~
{
  "dependencies": {
    "sqlite3": "5.1.6",
    "zod": "3.22.4"
  }
}
```

### 22.9.2 Reproducible builds

```bash
# Docker-контейнер для сборки с фиксированным toolchain
docker run --rm -v $(pwd):/src core/builder:v1.0.0 \
  cargo build --release --locked

# Проверка reproducibility
sha256sum target/release/workspace > build.hash
# Два разных разработчика → одинаковый build.hash
```

---

## 22.10. Интеграция с CI/CD

### 22.10.1 Security gates

```yaml
# .github/workflows/security.yml
jobs:
  seccomp_validate:
    runs-on: ubuntu-latest
    steps:
      - run: seccomp-tools dump ./target/release/workspace | diff - expected_seccomp.out
      
  namespace_test:
    runs-on: ubuntu-latest
    steps:
      - run: cargo test --test namespace_isolation
      
  v8_sandbox_test:
    runs-on: ubuntu-latest
    steps:
      - run: cargo test --test v8_sandbox_escape
      
  threat_model_check:
    runs-on: ubuntu-latest
    steps:
      - run: python scripts/check_threat_model.py --diff HEAD~1
```

---

## 22.11. Роадмап реализации

| Фаза | Что | Срок |
|------|-----|------|
| MVP-1 | Base seccomp + PID namespace + V8 Sandbox | Месяц 1–2 |
| MVP-2 | Network namespace + Micro-Kernel proxy | Месяц 2–3 |
| MVP-3 | Mount namespace + fs isolation | Месяц 3 |
| Post-MVP | Windows AppContainer + macOS seatbelt | Месяц 4–6 |
| Post-MVP | Encrypted memory + mlock max level | Месяц 4–6 |
| Post-MVP | Boot integrity + remote attestation | Месяц 5–7 |


## Связь с другими слоями

| Слой | Что оставлено |
|------|--------------|
| [Layer: UX](layer-1-user-experience.md) | «Безопасность — без паролей» — пользовательский опыт |
| [Layer: Фронт/Бэк](layer-3-system-split.md) | «Принцип безопасности Фронта» — почему Фронт не контролирует доступ |
| [Layer: Установка](layer-4-installation-scenarios.md) | Сценарии установки — бэкап, обновления, техподдержка в контексте каждого сценария |
| [Layer: Подсистемы](layer-8-technical-decomposition.md) | Security-аспекты подсистем (Key Manager, Auth Proxy, RBAC Engine, Audit Engine, Clipboard) |
| [Layer: Apps](layer-6-apps.md) | Security model по уровням приложений (sandbox, permissions, app-scoped data) |

---

## Предыдущий слой

Layer 6 описывает модель приложений — 5 уровней интеграции, манифест `core.json`, App Registry, обновления и безопасность приложений. [См. layer-6-apps.md](layer-6-apps.md).

---

## Следующий слой

Layer 8 описывает техническую декомпозицию подсистем — какие компоненты входят во Фронт и Бэк, как данные текут между ними, на каком уровне архитектуры каждая живёт. [См. layer-8-technical-decomposition.md](layer-8-technical-decomposition.md).
